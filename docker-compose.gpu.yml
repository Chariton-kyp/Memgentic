# GPU override — use with: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d
# Enables NVIDIA GPU passthrough for Ollama (requires NVIDIA Container Toolkit)
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
